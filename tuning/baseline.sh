#!/usr/bin/env bash
# baseline: stock kernel defaults, no tuning.
# This is the control. Run this before daily to measure what
# a fresh instance gives you out of the box.

echo "config: baseline (no tuning)"
